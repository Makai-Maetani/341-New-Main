<div class="top-controls">
  <% if (user) { %>
    <span>Welcome, <%= user.username %>!</span>
    <a href="/logout" class="genButtton">Logout</a>
  <% } else { %>
    <a href="/signup" class="genButtton">Sign In / Up</a>
  <% } %>
  <button class="add-contact-btn">+ Add Recipe</button>
</div>
